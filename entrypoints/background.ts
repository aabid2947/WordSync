// Background service worker.
// CP3 wires the DB writer, content-script hydration, learn-event batching,
// and offscreen/LLM orchestration here.
export default defineBackground(() => {
  // Intentionally empty for CP0 — scaffold only.
});
